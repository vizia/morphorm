{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Basic",
            "program": "${workspaceFolder}/target/debug/examples/basic",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}